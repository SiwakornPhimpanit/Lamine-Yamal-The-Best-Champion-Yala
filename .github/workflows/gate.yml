name: Deploy Gate Simulation
 
on:
  push:
 
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Siwakorn Phimpanit
        run: |
          echo "กำลังรัน test..."
          exit 0        # exit 0 = test ผ่าน / exit 1 = test พัง
 
  deploy:
    needs: test                   
    runs-on: ubuntu-latest
    steps:
      - name: Fake deploy
        run: echo "🚀 Deploy ขึ้น production สำเร็จ!"
